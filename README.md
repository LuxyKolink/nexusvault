# nexusvault
Sistema de archivo distribuido con un servidor de aplicaciones central basado en SOAP que integra autenticación JWT (RMI), nodos de almacenamiento (gRPC/Go), y gestión de metadatos (NestJS/PostgreSQL). Ofrece replicación de archivos, transferencia segura en base64 y una API completa para operaciones sobre archivos.
